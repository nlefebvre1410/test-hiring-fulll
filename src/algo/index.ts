/**
 * Main entry point for the FizzBuzz application
 * Exports all types, constants, utilities, and services
 */
import { displayFizzBuzz } from './services/fizzbuzz.service';

// Exécuter FizzBuzz jusqu'à 100
displayFizzBuzz(100);
